READY TO PUSH - Fixed layout issue

Changes:
1. Reverted from CarouselStrip component back to inline grid + scroll CSS
2. "Which course" section: pwap-collage now has pwap-collage--scroll class
   - Keeps same images, same layout width/height
   - Just adds horizontal scroll: flex + overflow-x: auto
   - No page layout changes

3. "Proof of work" section: pwap-testimonials__scroll div
   - 6 client photos, horizontal scroll
   - No grid restructuring, just flex layout

4. CSS: Simple overflow-x: auto on flex containers
   - Hides scrollbar with scrollbar-width: none
   - 280px desktop, 240px mobile items

Page layout is UNCHANGED. Only photos scroll left/right.

PUSH:
git add -A
git commit -m "fix: Make PWAP photo sections horizontally scrollable without layout changes"
git push
